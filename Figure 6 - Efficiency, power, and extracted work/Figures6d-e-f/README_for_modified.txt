The data is written "modified" because the code used the correct full tau duration, but the data was generated using the duration of the compression, so there was a small debuging process during the plot generation where we have adjusted the tau values in the x-axis of these plots.

The legend in the plots appearing in the main draft can be obtained by uncommenting the legends in the python code.
